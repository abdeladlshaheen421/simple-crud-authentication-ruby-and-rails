<%= form_with(model: dsl) do |form| %>
  <% if dsl.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dsl.errors.count, "error") %> prohibited this dsl from being saved:</h2>

      <ul>
        <% dsl.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :field, style: "display: block" %>
    <%= form.text_field :field %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
